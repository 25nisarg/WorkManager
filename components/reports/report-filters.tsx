import type { ReportRange } from "@/types/report";

const fieldClass = "h-10 rounded-lg border border-slate-300 bg-white px-3 text-sm text-slate-700 outline-none focus:border-indigo-500 focus:ring-4 focus:ring-indigo-500/10";

export function ReportFilters({ range }: { range: ReportRange }) {
  return <form action="/reports" className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex flex-col gap-3 lg:flex-row lg:items-end"><div className="flex-1"><label htmlFor="report-preset" className="mb-1.5 block text-xs font-semibold uppercase tracking-wide text-slate-500">Date range</label><select id="report-preset" name="range" defaultValue={range.preset} className={fieldClass + " w-full"}><option value="this_month">This Month</option><option value="last_month">Last Month</option><option value="this_year">This Year</option><option value="custom">Custom Date Range</option></select></div><div><label htmlFor="report-start" className="mb-1.5 block text-xs font-semibold uppercase tracking-wide text-slate-500">Custom start</label><input id="report-start" name="start" type="date" defaultValue={range.preset === "custom" ? range.start : ""} className={fieldClass} /></div><div><label htmlFor="report-end" className="mb-1.5 block text-xs font-semibold uppercase tracking-wide text-slate-500">Custom end</label><input id="report-end" name="end" type="date" defaultValue={range.preset === "custom" ? range.end : ""} className={fieldClass} /></div><button type="submit" className="h-10 rounded-lg bg-slate-900 px-4 text-sm font-semibold text-white hover:bg-slate-700">Update report</button></div><p className="mt-3 text-xs text-slate-500">Work metrics use assignment received dates. Cash metrics use payment and expense transaction dates.</p></form>;
}
